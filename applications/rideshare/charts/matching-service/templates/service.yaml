# apiVersion: v1
# kind: Service
# metadata:
#   name: matching-service
#   namespace: rideshare
# spec:
#   selector: { app: matching-service }
#   ports:
#     - name: tcp
#       port: 3004
#       targetPort: 3004

# ---
apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: {{ .Release.Namespace }}
spec:
  selector: { app: matching-service }
  ports: [{ name: tcp, port: 3004, targetPort: 3004 }]
