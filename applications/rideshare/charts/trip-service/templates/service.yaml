apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: rideshare
spec:
  selector: { app: trip-service }
  ports:
    - { name: http, port: 3005, targetPort: 3005 }
    - { name: ws, port: 3006, targetPort: 3006 }
