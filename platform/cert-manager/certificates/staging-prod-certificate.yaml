apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rideshare-prod-use1-tls
  namespace: rideshare

spec:
  secretName: rideshare-prod-use1-tls

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

  dnsNames:
    - rideshare.lukmonadeokun.com
